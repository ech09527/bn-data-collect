# 币安 API 与 WebSocket（含永续合约）
python-binance>=1.0.20

# 异步 Kafka 客户端（cramjam 提供 lz4/snappy/zstd 压缩，Docker 下需安装以使用 kafka_compression_type）
aiokafka>=0.10.0
cramjam

# 配置管理（支持 toml、env、.secrets）
dynaconf>=3.2.0
